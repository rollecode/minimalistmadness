<?php
/**
 * Main loop for the index view. Renders the shared template part so the classic PHP templates
 * and the block templates output identical markup.
 *
 * @package minimalistmadness
 */

get_template_part( 'template-parts/loop-index' );
